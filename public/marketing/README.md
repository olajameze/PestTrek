# Marketing screenshots

Drop product screenshots here and set `imageSrc` in `lib/marketing/productProof.ts`.

Suggested filenames:

- `logbook-mobile.png` — field logbook on phone
- `dashboard.png` — owner dashboard overview
- `audit-report.png` — compliance report export preview

Use WebP or PNG. Recommended width: 1280px.
